package mobile 

import (
    "fmt"
)

func startMobileLocation() {
    fmt.Println("starting mobile location")
    fmt.Println("looking for connected mobiles")

    //print current clients
    //send a message to an specfic client and wait for him to accept (or not)
    //once the mobile user accepts then start receiving its position
    //and to send that position to the py client
    
}
